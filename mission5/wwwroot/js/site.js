// Please see documentation at https://learn.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.
////document.getelementbyid("btnsubmit").addeventlistener("click", function () {
////    const hoursinput = document.getelementbyid("hoursinput");
////    const outputbox = document.getelementbyid("outputbox");
////    const rateperhour = 15;

////    const hours = parseint(hoursinput.value);

////     validation to make sure they entered a positive number
////    if (isnan(hours) || hours <= 0) {
////        outputbox.textcontent = "please enter a valid number of hours greater than 0.";
////    } else {
////        const total = hours * rateperhour;
////        outputbox.textcontent = `total cost: $${total}`;
////   }
////});

// Write your JavaScript code.


// Wait for the DOM to be fully loaded
//$(document).ready(function () {
//    // Attach a click event handler to the button using jQuery
//    $("#btnSubmit").click(function () {
//        const hoursInput = $("#hoursInput");
//        const outputBox = $("#outputBox");
//        const ratePerHour = 15;

//        const hours = parseInt(hoursInput.val());

//        // Validation to make sure they entered a positive number
//        if (isNaN(hours) || hours <= 0) {
//            outputBox.text("Please enter a valid number of hours greater than 0.");
//        } else {
//            const total = hours * ratePerHour;
//            outputBox.text(`Total cost: $${total}`);
//        }
//    });
//});
